/**
 *
 */

const api = () => {
  const apiData = () => {
    console.log()
    return new Promise((resolve, reject) => {
      resolve({})
    })
  }
  return {
    apiData
  }
}

export default api
